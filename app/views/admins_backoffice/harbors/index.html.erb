
<h1>Listando Portos</h1>
<div class="table-responsive">

  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Id</th>
        <th>Nome</th>
        <th>Cidade</th>
        <th>Estado</th>

        
        <th>
          <%= link_to new_admins_backoffice_harbor_path, class:"btn btn-success btn-circle  " do %>
            <i class="fa fa-plus"></i>
          <% end %>
        </th>
      </tr>
    </thead>
    <tbody>
      <% @harbors.each do |harbor| %>
        <tr>
          <td><%= harbor.id %></td>
          <td><%= harbor.name %></td>
          <td><%= harbor.city %></td>
          <td><%= harbor.state %></td>

            <td style="width: 150px">
              <%=link_to edit_admins_backoffice_harbor_path(harbor.id), class: 'btn btn-primary btn-circle btn-sm'   do%>
                <i class="fas fa-edit"></i> Edit
              <%end%>                           

              <%=link_to admins_backoffice_harbor_path(harbor.id),method: :delete, class: 'btn btn-danger btn-sm',data:{confirm: t('messages.confirm_with', item: harbor.name)} do%>
                  <i class="fas fa-trash"></i> Del
              <%end%>
            </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>


<div class = "d-flex justify-content-center"> 
  <%# <%= paginate @users %> 
</div>